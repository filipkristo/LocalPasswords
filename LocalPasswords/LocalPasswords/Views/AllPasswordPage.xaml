<Page
    x:Class="LocalPasswords.Views.AllPasswordPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalPasswords.Views"
    xmlns:ctl="using:LocalPasswords.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ctl:PageHeader>
            <ctl:PageHeader.HeaderContent>
                <TextBlock x:Uid="AllPasswordsHeader" x:Name="title" Style="{ThemeResource PageTitleTextBlockStyle}" Text="All passwords"/>
            </ctl:PageHeader.HeaderContent>
        </ctl:PageHeader>

        <StackPanel Grid.Row="1">

        </StackPanel>
        
    </Grid>
</Page>
