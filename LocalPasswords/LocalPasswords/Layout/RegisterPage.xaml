<Page
    x:Class="LocalPasswords.Layout.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalPasswords.Layout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"  >
            <TextBlock>Password:</TextBlock>
            <PasswordBox Password="{x:Bind ViewModel.Model.MasterPassword, Mode=TwoWay}" MinWidth="300" Width="100%" Margin="0,5,0,0"/>
            <TextBlock Margin="0,10,0,0">Confirm password:</TextBlock>
            <PasswordBox Password="{x:Bind ViewModel.Model.ConfirmPassword, Mode=TwoWay}" Margin="0,5,0,0" MinWidth="300" Width="100%"/>
            <Button Command="{Binding RegisterCommand}" Content="Set password" HorizontalAlignment="Center"  Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</Page>
