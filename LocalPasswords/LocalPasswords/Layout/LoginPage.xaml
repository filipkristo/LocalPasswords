<Page
    x:Class="LocalPasswords.Layout.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalPasswords.Layout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock>Password:</TextBlock>
            <PasswordBox Password="{x:Bind ViewModel.Model.MasterPassword, Mode=TwoWay}" MinWidth="300" Width="100%" Margin="0,5,0,0"/>
            <Button Command="{Binding LoginCommand}" Content="Enter" HorizontalAlignment="Center"  Margin="0,15,0,0"/>
        </StackPanel>
        
    </Grid>
</Page>
